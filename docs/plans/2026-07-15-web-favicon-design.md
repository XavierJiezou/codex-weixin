# Web favicon 设计

- 浏览器 favicon 使用 SVG，保持小尺寸下的清晰度并避免增加图片依赖。
- 图形复用页面左上角品牌标记：深色圆角终端方块、白色终端符号和右下角绿色聊天气泡。
- HTML 明确声明 `/favicon.svg`，本机静态服务返回 `image/svg+xml` 且保持 `no-store`，方便开发时立即刷新。
- 中英文 README 直接引用同一个 SVG 文件并以 128 × 128 居中展示，避免维护另一套品牌图形。
- 自动化测试覆盖页面声明、资源响应状态、内容类型和 SVG 品牌标题。
